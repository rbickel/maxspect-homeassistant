[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
markers =
    integration: tests requiring the full HA runtime (pytest-homeassistant-custom-component)
    protocol: Layer 3 protocol simulation tests (real TCP, no HA runtime)
    enable_socket: tests that require real network socket access
